# libnfs pkg-config file

prefix=/home/zouyingjun/work-place/nfs/prefix
exec_prefix=${prefix}
libdir=${exec_prefix}/lib
includedir=${prefix}/include

Name: libnfs
Description: libnfs is a client library for accessing NFS shares over a network.
Version: 2.0.0
Requires:
Conflicts:
Libs: -L${libdir} -lnfs
Cflags: -I${includedir}
